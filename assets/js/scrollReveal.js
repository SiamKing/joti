$(function() {
  window.sr = ScrollReveal();
  sr.reveal('.joti-hero-text', { duration: 2000}, 50);
  sr.reveal('.chevron');
  // sr.reveal('h1');
  // sr.reveal('.icon', { rotate: {x: 65}});
  // sr.reveal('h3');
  // sr.reveal('p');
  // sr.reveal('.pride-section');
  // sr.reveal('img');

})
